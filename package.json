{
  "private": true,
  "workspaces": ["packages/*", "playground"],
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "bun run --filter '@zojize/dead-drop' test",
    "typecheck": "bun run --filter '*' typecheck",
    "knip": "knip"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^8.0.0",
    "@eslint-react/eslint-plugin": "^3.0.0",
    "eslint": "^10.2.0",
    "eslint-plugin-react-refresh": "^0.5.2",
    "knip": "^5.61.0"
  }
}
